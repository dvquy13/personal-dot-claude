Merge the $ARGUMENTS branch with squash commit and clean up the worktree and branch.

Study the commit messages to compose a summary in the squash commit message. If there is only one commit, then merge as is (no need to squash) using: `git merge <branch>`.

Resolve the conflicts if any. If unsure about the changes, ask for human help.

After merging completes, delete the worktree and the branch.
