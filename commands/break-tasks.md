Break the task/milestones in $ARGUMENTS into their own markdown files to keep track of progress separately.

The new sub-task files should be placed in $ARGUMENTS/ folder. For example, if the input file is `.lpm/set-up.md`, then the new sub-task files should be placed in `.lpm/set-up/` folder.

Migrate the milestone/task details in $ARGUMENTS file to the new sub-task files and update the $ARGUMENTS file to reference the new sub-task files instead of keeping the details.
